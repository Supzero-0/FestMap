# ==============================================================================
#                      FestMap Environment Variables
#
# Cet exemple liste toutes les variables d'environnement nécessaires
# pour faire fonctionner l'application dans différents contextes (local, CI, prod).
# Copiez ce fichier en .env et remplissez-le pour votre environnement local.
# En production (Render/Vercel) et en CI (GitHub Actions), ces variables
# doivent être configurées directement dans les paramètres du service.
# ==============================================================================


# ==============================================================================
# ==                         LOCAL & DOCKER-COMPOSE                           ==
# ==============================================================================
# Utilisé pour le développement local (lancement direct ou via `docker-compose up`)

# Profil Spring actif
# 'dev' est le profil par défaut, mais on le laisse pour la clarté.
SPRING_PROFILES_ACTIVE=dev

# Configuration de la base de données (utilisée par `application.yml`)
# Pour `docker-compose`, l'hôte est le nom du service, ex: `db`.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=festmap
DB_USER=festmap
DB_PASSWORD=password

# Ports des services
API_PORT=8080
CLIENT_PORT=4200


# ==============================================================================
# ==                        PRODUCTION (Render / Vercel)                      ==
# ==============================================================================
# Variables à configurer dans les "Environment Variables" de vos services.

# --- Backend (Render) ---

# Active le profil de production de Spring Boot
SPRING_PROFILES_ACTIVE=prod

# Variables de connexion à la base de données PostgreSQL de Render.
# Exemple: jdbc:postgresql://hostname:port/dbname
SPRING_DATASOURCE_URL= # Ex: jdbc:postgresql://...
SPRING_DATASOURCE_USERNAME= # Secret
SPRING_DATASOURCE_PASSWORD= # Secret

# Origines autorisées pour les requêtes Cross-Origin (CORS).
# Doit être l'URL de votre frontend Vercel.
# Exemple: https://mon-app-vercel.vercel.app
CORS_ALLOWED_ORIGINS= # Secret

# Secret pour la génération et validation des tokens JWT.
# Doit être une chaîne de caractères longue et aléatoire.
JWT_SECRET= # Secret

# --- Frontend (Vercel) ---

# URL de l'API backend que le client Angular doit appeler.
# En production, pointe vers votre service backend Render.
# Exemple: https://mon-app-render.onrender.com
# La variable est souvent préfixée (ex: NG_APP_API_URL, VITE_API_URL).
# À adapter selon la configuration du build Angular.
NG_APP_API_URL= # Ex: https://festmap-api.onrender.com
