databaseChangeLog:
  - changeSet:
      id: 006-add-role-to-user-table
      author: supzero
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: role
                  type: VARCHAR(20)
                  defaultValue: 'USER'
                  constraints:
                    nullable: false
